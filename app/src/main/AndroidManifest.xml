<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidninja.myfragment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFragment">
        <activity android:name=".masterslave.MasterActivity"></activity>
        <activity android:name=".StaticFragmentActivity" />
        <activity android:name=".DynamicFragmentActivity" />
        <activity android:name=".LifeCycleFragmentActivity" />
        <activity android:name=".backstack.BackStackFragmentActivity" />
        <activity android:name=".backstack1.AddPopReplaceBackStackFragmentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>